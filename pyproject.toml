[project]
name = "notes"
version = "0.1.0"
description = "note manage tools"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "frontmatter>=3.0.8",
    "pathlib>=1.0.1",
    "uuid>=1.30",
]
